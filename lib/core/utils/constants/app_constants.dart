class AppConstants {
  AppConstants._();

  //? Base URL
  static String get baseUrl => 'https://ecommerce.routemisr.com/api/v1/';
  // static const Duration s3SendTimeout = Duration(minutes: 20);
  // static const Duration s3ReceiveTimeout = Duration(minutes: 20);
}
